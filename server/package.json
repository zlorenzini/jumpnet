{
  "name": "jumpnet-server",
  "version": "0.1.0",
  "description": "JumpNet — AI inference gateway for JumpOS",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev":   "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer":  "^1.4.5-lts.1"
  },
  "engines": {
    "node": ">=20"
  }
}
